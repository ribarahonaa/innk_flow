// Isla del editor del formulario de postulación.
import FormEditor from '../components/form_editor/form_editor.vue';
import { mountIsland } from '../islands';

mountIsland('form-editor', FormEditor);
