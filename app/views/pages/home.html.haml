.panel
  %h1 innk flow
  %p.muted
    = "#{current_company.name} · #{current_user.name} (#{current_membership.role})"
  %p Fase 1 — tenencia, sesión y semillas. El aislamiento por empresa está armado.
